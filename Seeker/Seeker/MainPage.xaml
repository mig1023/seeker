<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Class="Seeker.MainPage">
    <ScrollView>
        <StackLayout Padding="30">
            <Label x:Name="Title" Text="" Margin="5" FontSize="Large"/>
            <Label x:Name="Text" Text="" Margin="5" FontSize="Default"/>
            <StackLayout x:Name="Options" Orientation="Vertical" Spacing="5" HorizontalOptions="FillAndExpand" />
            
            
             <StackLayout Orientation="Horizontal">
              <Entry x:Name="fileNameEntry" HorizontalOptions="FillAndExpand" />
                <Button Text="Обновить" Clicked="OnAppearing" />
            </StackLayout>
            <Editor Grid.Row="1" x:Name="textEditor" />
            <ListView x:Name="filesList" Grid.Row="2" ItemSelected="FileSelect">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <TextCell Text="{Binding}">
                        </TextCell>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </StackLayout>
    </ScrollView>
</ContentPage>
